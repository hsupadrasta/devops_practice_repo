def highlight(element):
    driver.execute_script("""
        var elem = arguments[0];
        var original_style = elem.getAttribute('style');
        elem.setAttribute('style', original_style + '; border: 2px solid cyan;');
    """, element)

# Highlight all leaf elements
for element in leaf_elements:
    highlight(element)
